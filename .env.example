# Google Cloud console -> APIs -> enable "Map Tiles API" and "Places API (New)" -> create key.
# Set a daily quota cap and restrict the key to your site's domain BEFORE the first request.
VITE_GOOGLE_MAPS_KEY=
# Multiplayer room URL from `cd server && npx wrangler deploy`. Leave empty to play solo.
VITE_MP_URL=
